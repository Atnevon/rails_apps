<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @category.name %>
</p>

<p><strong>Products in here:</strong>
	<ul>
		<% @category.products.each do |product| %>
		<li><%= product.name %></li>
		<% end %>
	</ul>
</p>


<%= link_to 'Edit', edit_category_path(@category) %> |
<%= link_to 'Back', categories_path %>
